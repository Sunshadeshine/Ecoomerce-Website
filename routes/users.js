// import express from "express";
// // import {profile, orders, wallet } from "../controllers/home-controller.js";
// import { isAdmin, requireSignIn } from "../middlewares/auth_Middleware.js";
// const router = express.Router();
// //protected routes
// router.get("/user-auth", requireSignIn, (req, res) => {
//   res.send(200).send({ ok: true });
// });
// // router.get("/profile", requireSignIn, signUp);
// // router.get("/order", requireSignIn, signIn);
// // router.get("/wallet", requireSignIn, signIn);

// export default router;
